$addButtonProfile=document.getElementById("add-collection-access");if($addButtonProfile){$addButtonProfile.addEventListener("click",function(){let $addButtonProfile=this;let $blockCollection=document.getElementById($addButtonProfile.dataset.collection);let newForm=$addButtonProfile.dataset.prototype;let index=$addButtonProfile.dataset.index*1;newForm=newForm.replace(/__access__/g,index);let div=document.createElement("div");div.id=$addButtonProfile.dataset.item.replace(/__access__/g,index);div.innerHTML=newForm;$blockCollection.append(div);div.querySelector(".del-item")?.addEventListener("click",removeElement);new NiceSelect(div.querySelector('[data-select="select2"]'),{searchable:true});$addButtonProfile.dataset.index=(index+1).toString();div.scrollIntoView({block:"center",inline:"center",behavior:"smooth"})})}document.querySelectorAll(".del-item").forEach(function(item){item.addEventListener("click",removeElement)});function removeElement(){document.getElementById(this.dataset.delete).remove()}